<div class="container">
  <div class="title">
    <h3>Preciso de ajuda com...</h3>
  </div>
  <div class="row ">
    <% @app.each do |ap| %>
    <div class="col-lg-4 col-md-6 col-sm-12 mt-3" id="center">
      <div class="card-category">
        <%= link_to category_issues_path(@category) do %>
        <p><%= ap.name %></p>
        <% end %>
      </div>
    </div>
    <% end %>
  </div>
</div>
